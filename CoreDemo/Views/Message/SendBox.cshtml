@using EntityLayer.Concrete
@model List<Message2>
@{
    ViewData["Title"] = "SendBox";
    Layout = "~/Views/Shared/WriterLayout.cshtml";
}

<h1>Writer`s sendbox</h1>

<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>Subject</th>
        <th>Receiver</th>
        <th>Date</th>
        <th>Open Message</th>
    </tr>
    @foreach (var message in Model)
    {
        <tr>
            <th>@message.MessageId</th>
            <td>@message.Subject</td>
            <td>@message.ReceiverUser.WriterName</td>
            <td>@(((DateTime)message.MessageDate).ToString("dd-MMM-yyyy"))</td>
            <td><a href="/Message/MessageDetails/@message.MessageId" class="btn btn-warning">Open Message</a></td>
        </tr>
    }
</table>

<a href="/Message/SendMessage/" class="btn btn-primary">Add new message</a>



